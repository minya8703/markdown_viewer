# UI/UX 설계서

## 문서 정보
- **프로젝트**: 마크다운 뷰어 V2
- **버전**: 1.0
- **작성일**: 2026-01-29
- **기반 문서**: SYSTEM_ARCHITECTURE.md

## 목차
1. [개요](#개요)
2. [디자인 원칙](#디자인-원칙)
3. [화면 구성](#화면-구성)
4. [컴포넌트 설계](#컴포넌트-설계)
5. [사용자 흐름](#사용자-흐름)
6. [반응형 디자인](#반응형-디자인)
7. [접근성](#접근성)
8. [암호화 관련 UI/UX 상세 설계](#암호화-관련-uiux-상세-설계)
9. [암호화 사용자 시나리오](#암호화-사용자-시나리오)

---

## 개요

### 목표
- **단일 화면 중심**: 파일을 열면 즉시 마크다운 콘텐츠에 집중
- **직관적인 아이콘 기반 UI**: 텍스트 대신 아이콘으로 기능 표시
- **편안한 사용자 경험**: 최소한의 클릭으로 작업 완료
- **국제적 접근성**: 언어에 관계없이 이해 가능한 UI

### 디자인 철학
- **Minimalism**: 불필요한 요소 제거
- **Content First**: 콘텐츠에 집중
- **Intuition**: 직관적인 인터페이스
- **Consistency**: 일관된 디자인 패턴

---

## 디자인 원칙

### 1. 단일 화면 중심
- 파일을 열면 즉시 마크다운 콘텐츠 표시
- 파일 탐색기는 필요 시에만 표시
- 편집 모드는 전체 화면으로 전환

### 2. 아이콘 기반 UI
- Font Awesome 아이콘 사용
- 모든 기능을 아이콘으로 표시
- 툴팁으로 기능 설명 제공

### 3. 블랙 앤 화이트 테마
- 깔끔한 흑백 디자인
- 색상은 최소한으로 사용 (상태 표시용)
- 고대비로 가독성 향상

### 4. 반응형 디자인
- 데스크톱, 태블릿, 모바일 지원
- 화면 크기에 따라 레이아웃 조정
- 터치 친화적 인터페이스

---

## 화면 구성

### 전체 레이아웃

```
┌─────────────────────────────────────────────────────┐
│ Header (고정)                                       │
│ ┌─────────┐ ┌──────────┐ ┌────┐ ┌────┐ ┌────┐    │
│ │ [Menu]  │ │ FileName │ │[Edit]│[Save]│[⚙] │    │
│ └─────────┘ └──────────┘ └────┘ └────┘ └────┘    │
├─────────────────────────────────────────────────────┤
│                                                     │
│ Main Content Area (가변)                           │
│ ┌───────────────────────────────────────────────┐  │
│ │                                               │  │
│ │         Markdown Content                     │  │
│ │         (중앙 정렬, 최대 너비 제한)          │  │
│ │                                               │  │
│ │  # Document                                   │  │
│ │                                               │  │
│ │  Content here...                              │  │
│ │                                               │  │
│ └───────────────────────────────────────────────┘  │
│                                                     │
├─────────────────────────────────────────────────────┤
│ Footer (고정)                                       │
│ ┌───────────────────────────────────────────────┐ │
│ │            Google AdSense                     │ │
│ └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

---

## 컴포넌트 설계

### 1. 암호화 다이얼로그

#### 구성 요소
```
┌─────────────────────────────────────────┐
│ 🔒 파일 암호화                    [×]   │
├─────────────────────────────────────────┤
│                                         │
│ 파일 "document.md"을(를) 암호화하여     │
│ 저장합니다.                             │
│                                         │
│ ⚠️ 비밀번호를 잊어버리면 파일을 복구할  │
│ 수 없습니다. 안전한 곳에 보관하세요.    │
│                                         │
│ 비밀번호                                │
│ ┌───────────────────────────────────┐  │
│ │ ••••••••                          │  │
│ └───────────────────────────────────┘  │
│ [약함] 비밀번호는 최소 8자 이상...      │
│                                         │
│ 비밀번호 확인                           │
│ ┌───────────────────────────────────┐  │
│ │ ••••••••                          │  │
│ └───────────────────────────────────┘  │
│                                         │
│ ☑ 이 비밀번호를 기억하기                │
│   (localStorage에 저장)                 │
│                                         │
├─────────────────────────────────────────┤
│              [취소]  [암호화하여 저장]  │
└─────────────────────────────────────────┘
```

**요소 설명:**
- **제목**: "파일 암호화" 아이콘과 함께 표시
- **파일명 표시**: 현재 저장할 파일명 강조 표시
- **경고 메시지**: 비밀번호 분실 시 복구 불가 안내
- **비밀번호 입력**: 
  - 실시간 강도 표시 (약함/보통/강함)
  - 강도에 따라 색상 변경 (빨강/주황/초록)
- **비밀번호 확인**: 일치 여부 실시간 검증
- **비밀번호 기억하기**: localStorage에 저장 옵션
- **버튼**: 취소 / 암호화하여 저장 (비밀번호 일치 시 활성화)

**상태 표시:**
- 비밀번호 강도:
  - 약함: 빨간색 배경, "비밀번호는 최소 8자 이상이어야 합니다."
  - 보통: 주황색 배경, "보통 강도의 비밀번호입니다. 더 강력한 비밀번호를 권장합니다."
  - 강함: 초록색 배경, "강력한 비밀번호입니다."
- 비밀번호 불일치: 빨간색 에러 메시지 "비밀번호가 일치하지 않습니다."
- 저장 버튼: 비밀번호 일치 및 강도 검증 통과 시 활성화

**스타일:**
```css
.encryption-dialog {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: center;
}

.encryption-dialog__container {
  background: #ffffff;
  border-radius: 8px;
  max-width: 500px;
  width: 90%;
  box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
}

.encryption-dialog__strength--weak {
  color: #f44336;
  background: rgba(244, 67, 54, 0.1);
}

.encryption-dialog__strength--medium {
  color: #ff9800;
  background: rgba(255, 152, 0, 0.1);
}

.encryption-dialog__strength--strong {
  color: #4caf50;
  background: rgba(76, 175, 80, 0.1);
}
```

**키보드 단축키:**
- `Enter`: 비밀번호 일치 시 저장
- `Esc`: 취소

---

### 2. 복호화 다이얼로그

#### 구성 요소
```
┌─────────────────────────────────────────┐
│ 🔓 파일 복호화                    [×]   │
├─────────────────────────────────────────┤
│                                         │
│ 파일 "document.md"은(는) 암호화되어     │
│ 있습니다.                               │
│ 비밀번호를 입력하여 복호화하세요.       │
│                                         │
│ 비밀번호                                │
│ ┌───────────────────────────────────┐  │
│ │ ••••••••                          │  │
│ └───────────────────────────────────┘  │
│                                         │
│                                         │
├─────────────────────────────────────────┤
│              [취소]      [복호화]       │
└─────────────────────────────────────────┘
```

**요소 설명:**
- **제목**: "파일 복호화" 아이콘과 함께 표시
- **파일명 표시**: 복호화할 파일명 표시
- **비밀번호 입력**: 단일 입력 필드
- **에러 메시지**: 복호화 실패 시 표시
- **버튼**: 취소 / 복호화

**상태 표시:**
- 복호화 진행 중: 로딩 인디케이터
- 복호화 실패: 빨간색 에러 메시지 "비밀번호가 올바르지 않습니다."
- 복호화 성공: 다이얼로그 닫고 콘텐츠 표시

**스타일:**
- 암호화 다이얼로그와 동일한 스타일 사용
- 더 간단한 레이아웃 (비밀번호 확인 필드 없음)

**키보드 단축키:**
- `Enter`: 복호화 시도
- `Esc`: 취소

---

### 3. Header (헤더)

#### 구성 요소
```
┌──────────────────────────────────────────────────┐
│ [☰] Document.md        [✏️] [💾] [⚙️] [👤]     │
└──────────────────────────────────────────────────┘
```

**요소 설명:**
- `[☰]` (햄버거 메뉴): 사이드바 토글
- `Document.md`: 현재 파일명
- `[✏️]` (편집 아이콘): 편집 모드 진입
- `[💾]` (저장 아이콘): 파일 저장 (편집 모드에서만 표시)
- `[⚙️]` (설정 아이콘): 설정 메뉴
- `[👤]` (사용자 아이콘): 사용자 메뉴 (로그아웃 등)

**상태 표시:**
- 저장 상태: `[💾]` 옆에 텍스트 표시
  - "저장됨" (회색)
  - "저장 중..." (파란색)
  - "저장 실패" (빨간색)

**스타일:**
```css
.header {
  height: 60px;
  background: #ffffff;
  border-bottom: 1px solid #e0e0e0;
  display: flex;
  align-items: center;
  padding: 0 20px;
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 1000;
}
```

---

### 2. Sidebar (사이드바)

#### 구성 요소
```
┌─────────────────────┐
│  파일 관리          │
├─────────────────────┤
│ 📁 folder1         │
│   📄 file1.md      │
│   🔐 secret.md      │ (암호화됨)
│ 📄 document.md     │
│ 🔐 notes.md        │ (암호화됨)
├─────────────────────┤
│ [➕ 새 파일]       │
│ [📤 업로드]        │
└─────────────────────┘
```

**암호화된 파일 표시:**
- 파일명 앞에 🔐 아이콘 표시
- 호버 시 툴팁: "암호화된 파일"
- 클릭 시 복호화 다이얼로그 자동 표시

**기능:**
- 폴더 트리 구조 표시
- 파일 클릭 시 즉시 로드
- 새 파일 생성
- 파일 업로드
- 검색 기능

**위치:**
- 기본: 왼쪽 (숨김)
- 토글 가능
- 위치 변경 가능 (왼쪽/오른쪽)

**스타일:**
```css
.sidebar {
  width: 250px;
  background: #f5f5f5;
  border-right: 1px solid #e0e0e0;
  height: calc(100vh - 60px);
  overflow-y: auto;
  position: fixed;
  left: 0;
  top: 60px;
  transition: transform 0.3s ease;
}

.sidebar.hidden {
  transform: translateX(-100%);
}
```

---

### 3. Main Content Area (메인 콘텐츠 영역)

#### 뷰어 모드
```
┌─────────────────────────────────────────────┐
│                                             │
│         (여백)                              │
│                                             │
│    ┌─────────────────────────────────┐     │
│    │                                 │     │
│    │    # Document Title             │     │
│    │                                 │     │
│    │    Content here...              │     │
│    │                                 │     │
│    │    - List item 1                │     │
│    │    - List item 2                │     │
│    │                                 │     │
│    └─────────────────────────────────┘     │
│                                             │
│         (여백)                              │
│                                             │
└─────────────────────────────────────────────┘
```

**특징:**
- 중앙 정렬
- 최대 너비 제한 (예: 800px)
- 가독성 최적화
- 코드 하이라이팅 지원

**스타일:**
```css
.main-content {
  max-width: 800px;
  margin: 0 auto;
  padding: 40px 20px;
  line-height: 1.6;
  font-size: 16px;
}
```

#### 편집 모드
```
┌─────────────────────────────────────────────┐
│ [✏️] Document.md    [🔒] [💾] [❌]          │
├─────────────────────────────────────────────┤
│                                             │
│ ┌───────────────────────────────────────┐  │
│ │ # Document Title                      │  │
│ │                                       │  │
│ │ Content here...                       │  │
│ │                                       │  │
│ │ - List item 1                         │  │
│ │ - List item 2                         │  │
│ │                                       │  │
│ └───────────────────────────────────────┘  │
│                                             │
│ [저장됨] (3분 전)                           │
└─────────────────────────────────────────────┘
```

**특징:**
- 전체 화면 편집기
- 실시간 편집
- 저장 상태 표시
- 닫기 버튼
- **암호화 토글 버튼**: 편집 모드에서 암호화 저장 여부 선택
  - 클릭 시 암호화 다이얼로그 표시
  - 활성화 시 아이콘 강조 표시

---

### 4. Footer (푸터)

#### 구성 요소
```
┌─────────────────────────────────────────────┐
│                                             │
│         Google AdSense 광고 영역           │
│                                             │
└─────────────────────────────────────────────┘
```

**특징:**
- 고정 높이
- 반응형 광고
- 페이지 하단 고정

---

## 사용자 흐름

### 1. 로그인 흐름

```
[로그인 페이지]
    │
    ├─ [Google 로그인 버튼 클릭]
    │       │
    │       ▼
    │   [Google 인증]
    │       │
    │       ▼
    │   [메인 페이지로 리다이렉트]
    │       │
    │       ▼
    │   [마지막 문서 자동 로드]
    │       │
    │       ▼
    │   [뷰어 화면 표시]
    │
    └─ [로그인 없이 뷰어 시작 버튼 클릭]
            │
            ▼
        [뷰어 화면 표시 (비로그인)]
            │
            ▼
        [빈 화면 또는 로컬 파일 열기만 가능]
```

**로그인 페이지 UI 구성:**
- **Google로 로그인** 버튼 (primary): Google OAuth 진행 후 뷰어로 이동
- **로그인 없이 뷰어 시작** 버튼 (secondary): 로그인 없이 뷰어 페이지로 이동, 로컬 파일 열기/저장만 가능

**UI 상태:**
- 로그인 전: 로그인 페이지 (두 버튼 모두 표시)
- 비로그인 뷰어: 뷰어 화면, 사용자 아이콘 클릭 시 로그인 페이지로 이동
- 로그인 후: 헤더에 사용자 아이콘 표시, 서버 파일 목록·저장·업로드 사용 가능

---

### 2. 파일 열기 흐름

```
[햄버거 메뉴 클릭]
    │
    ▼
[사이드바 표시]
    │
    ▼
[파일 클릭]
    │
    ▼
[파일 로드 중...]
    │
    ▼
[마크다운 콘텐츠 표시]
```

**UI 피드백:**
- 파일 클릭 시 로딩 인디케이터 표시
- 로드 완료 시 콘텐츠 페이드인 애니메이션

---

### 3. 편집 흐름

```
[편집 아이콘 클릭]
    │
    ▼
[편집 모드로 전환]
    │
    ▼
[텍스트 편집]
    │
    ▼
[자동 저장 (3분마다)]
    │
    ▼
[저장 상태 표시]
    │
    ▼
[닫기 버튼 클릭]
    │
    ▼
[변경사항 확인 다이얼로그]
    │
    ├─ [저장 후 닫기]
    │   │
    │   ▼
    │   [뷰어 모드로 전환]
    │
    └─ [저장하지 않고 닫기]
        │
        ▼
        [뷰어 모드로 전환]
```

---

### 4. 파일 저장 흐름 (암호화 포함)

```
[저장 버튼 클릭 (Ctrl+S)]
    │
    ▼
[저장 옵션 선택]
    │
    ├─ [일반 저장]
    │   │
    │   ▼
    │   [서버에 평문 저장]
    │   │
    │   ▼
    │   [저장 완료]
    │
    └─ [암호화 저장]
        │
        ▼
        [암호화 다이얼로그 표시]
        │
        ├─ [비밀번호 입력]
        │   │
        │   ├─ [비밀번호 확인 입력]
        │   │   │
        │   │   ├─ [비밀번호 불일치]
        │   │   │   │
        │   │   │   ▼
        │   │   │   [에러 메시지 표시]
        │   │   │
        │   │   └─ [비밀번호 일치]
        │   │       │
        │   │       ▼
        │   │       [비밀번호 강도 표시]
        │   │       │
        │   │       ├─ [약함] (경고 표시)
        │   │       ├─ [보통] (권장)
        │   │       └─ [강함] (권장)
        │   │
        │   └─ [비밀번호 기억하기 체크]
        │       │
        │       ▼
        │       [localStorage에 저장]
        │
        ▼
        [암호화 진행]
        │
        ▼
        [암호화된 데이터 서버 전송]
        │
        ▼
        [저장 완료]
```

**UI 상태:**
- 저장 버튼 클릭 시: 저장 옵션 선택 메뉴 표시 (일반 저장 / 암호화 저장)
- 암호화 선택 시: 암호화 다이얼로그 모달 표시
- 비밀번호 입력 중: 실시간 강도 표시
- 암호화 진행 중: 로딩 인디케이터 표시

---

### 5. 암호화된 파일 읽기 흐름

```
[암호화된 파일 클릭]
    │
    ▼
[파일 메타데이터 확인]
    │
    ├─ [encrypted: false]
    │   │
    │   ▼
    │   [일반 파일 읽기]
    │
    └─ [encrypted: true]
        │
        ▼
        [복호화 다이얼로그 표시]
        │
        ├─ [localStorage에 저장된 비밀번호 확인]
        │   │
        │   ├─ [비밀번호 있음]
        │   │   │
        │   │   ▼
        │   │   [자동 복호화 시도]
        │   │   │
        │   │   ├─ [성공]
        │   │   │   │
        │   │   │   ▼
        │   │   │   [평문 콘텐츠 표시]
        │   │   │
        │   │   └─ [실패]
        │   │       │
        │   │       ▼
        │   │       [비밀번호 입력 요청]
        │   │
        │   └─ [비밀번호 없음]
        │       │
        │       ▼
        │       [비밀번호 입력 요청]
        │
        ▼
        [사용자 비밀번호 입력]
        │
        ▼
        [복호화 시도]
        │
        ├─ [성공]
        │   │
        │   ▼
        │   [평문 콘텐츠 표시]
        │   │
        │   ▼
        │   [비밀번호 기억하기 옵션]
        │
        └─ [실패]
            │
            ▼
            [에러 메시지: "비밀번호가 올바르지 않습니다"]
            │
            ▼
            [다시 입력 또는 취소]
```

**UI 상태:**
- 암호화된 파일 감지 시: 자동으로 복호화 다이얼로그 표시
- 복호화 진행 중: 로딩 인디케이터 표시
- 복호화 실패 시: 에러 메시지 및 재시도 옵션

---

## 반응형 디자인

### 브레이크포인트

```css
/* 모바일 */
@media (max-width: 768px) {
  .sidebar {
    width: 100%;
    transform: translateX(-100%);
  }
  
  .main-content {
    padding: 20px 10px;
  }
}

/* 태블릿 */
@media (min-width: 769px) and (max-width: 1024px) {
  .sidebar {
    width: 200px;
  }
}

/* 데스크톱 */
@media (min-width: 1025px) {
  .sidebar {
    width: 250px;
  }
}
```

### 모바일 최적화
- 햄버거 메뉴로 사이드바 토글
- 터치 친화적 버튼 크기 (최소 44x44px)
- 스와이프 제스처 지원

---

## 접근성

### 키보드 네비게이션
- `Tab`: 다음 요소로 이동
- `Shift + Tab`: 이전 요소로 이동
- `Enter`: 버튼/링크 활성화
- `Esc`: 모달/사이드바 닫기
- `Ctrl + S`: 저장

### ARIA 레이블
```html
<button aria-label="파일 저장">
  <i class="fas fa-save"></i>
</button>

<nav aria-label="파일 탐색기">
  <!-- 파일 목록 -->
</nav>
```

### 스크린 리더 지원
- 모든 아이콘 버튼에 텍스트 레이블 제공
- 상태 변경 시 알림 제공
- 포커스 관리

---

## 아이콘 매핑

### 주요 기능 아이콘

| 기능 | 아이콘 | Font Awesome 클래스 |
|------|--------|-------------------|
| 메뉴 | ☰ | `fa-bars` |
| 편집 | ✏️ | `fa-edit` |
| 저장 | 💾 | `fa-save` |
| 설정 | ⚙️ | `fa-cog` |
| 사용자 | 👤 | `fa-user` |
| 파일 | 📄 | `fa-file` |
| 폴더 | 📁 | `fa-folder` |
| 새 파일 | ➕ | `fa-plus` |
| 업로드 | 📤 | `fa-upload` |
| 닫기 | ❌ | `fa-times` |
| 검색 | 🔍 | `fa-search` |
| 암호화 | 🔒 | `fa-lock` |
| 복호화 | 🔓 | `fa-unlock` |
| 암호화됨 | 🔐 | `fa-lock` (파일 목록에서 표시) |

---

## 색상 팔레트

### 기본 색상
```css
:root {
  /* 텍스트 */
  --text-primary: #000000;
  --text-secondary: #666666;
  --text-disabled: #999999;
  
  /* 배경 */
  --bg-primary: #ffffff;
  --bg-secondary: #f5f5f5;
  --bg-hover: #e0e0e0;
  
  /* 테두리 */
  --border-color: #e0e0e0;
  
  /* 상태 */
  --status-success: #4caf50;
  --status-warning: #ff9800;
  --status-error: #f44336;
  --status-info: #2196f3;
}
```

---

## 애니메이션

### 전환 효과
```css
/* 페이드 인 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* 슬라이드 인 */
@keyframes slideIn {
  from { transform: translateX(-100%); }
  to { transform: translateX(0); }
}

/* 적용 */
.content {
  animation: fadeIn 0.3s ease;
}

.sidebar {
  transition: transform 0.3s ease;
}
```

---

## 암호화 관련 UI/UX 상세 설계

### 1. 파일 저장 시 암호화 옵션 선택

#### 저장 버튼 동작
- **일반 저장**: 저장 버튼 클릭 시 즉시 저장
- **암호화 저장**: 
  - 방법 1: 저장 버튼 우클릭 → "암호화하여 저장" 선택
  - 방법 2: 편집 모드에서 암호화 토글 버튼 활성화 후 저장
  - 방법 3: 저장 버튼 옆 드롭다운 메뉴에서 선택

#### 저장 옵션 메뉴 (드롭다운)
```
┌──────────────────────┐
│ 💾 일반 저장         │
│ 🔒 암호화하여 저장   │
└──────────────────────┘
```

**동작:**
- "일반 저장" 클릭: 즉시 서버에 평문 저장
- "암호화하여 저장" 클릭: 암호화 다이얼로그 표시

---

### 2. 암호화 다이얼로그 상세 설계

#### 레이아웃 구조
```
┌─────────────────────────────────────────────┐
│ Header (고정)                               │
│ - 제목: "파일 암호화" + 아이콘              │
│ - 닫기 버튼 (X)                             │
├─────────────────────────────────────────────┤
│ Content (스크롤 가능)                       │
│ - 파일명 표시 영역                          │
│ - 경고 메시지 영역                          │
│ - 비밀번호 입력 필드                        │
│ - 비밀번호 강도 표시                        │
│ - 비밀번호 확인 필드                        │
│ - 에러 메시지 영역                          │
│ - 비밀번호 기억하기 체크박스                │
├─────────────────────────────────────────────┤
│ Footer (고정)                               │
│ - 취소 버튼                                 │
│ - 암호화하여 저장 버튼 (비활성화 가능)     │
└─────────────────────────────────────────────┘
```

#### 비밀번호 강도 표시 규칙

**약함 (Weak):**
- 조건: 길이 < 8자 또는 단일 문자 유형만 사용
- 색상: 빨간색 (#f44336)
- 배경: 연한 빨간색 (rgba(244, 67, 54, 0.1))
- 메시지: "비밀번호는 최소 8자 이상이어야 합니다."
- 저장 버튼: 비활성화

**보통 (Medium):**
- 조건: 길이 >= 8자, 2-3가지 문자 유형 조합
- 색상: 주황색 (#ff9800)
- 배경: 연한 주황색 (rgba(255, 152, 0, 0.1))
- 메시지: "보통 강도의 비밀번호입니다. 더 강력한 비밀번호를 권장합니다."
- 저장 버튼: 활성화

**강함 (Strong):**
- 조건: 길이 >= 12자, 4가지 이상 문자 유형 조합 (대문자, 소문자, 숫자, 특수문자)
- 색상: 초록색 (#4caf50)
- 배경: 연한 초록색 (rgba(76, 175, 80, 0.1))
- 메시지: "강력한 비밀번호입니다."
- 저장 버튼: 활성화

#### 비밀번호 확인 검증
- 실시간 검증: 비밀번호 확인 입력 시 즉시 일치 여부 확인
- 불일치 시: 빨간색 에러 메시지 "비밀번호가 일치하지 않습니다."
- 일치 시: 에러 메시지 숨김, 저장 버튼 활성화

#### 비밀번호 기억하기 기능
- 체크박스: "이 비밀번호를 기억하기 (localStorage에 저장)"
- 저장 위치: `localStorage.setItem('encryption_password_${filePath}', password)`
- 보안 고려사항:
  - 비밀번호는 암호화하지 않고 저장 (사용자 선택)
  - 경고 메시지: "다른 사용자가 이 컴퓨터를 사용할 수 있습니다."
- 사용 시나리오:
  - 같은 파일을 다시 열 때 자동으로 비밀번호 입력
  - 다른 파일 암호화 시 기본값으로 제안

---

### 3. 복호화 다이얼로그 상세 설계

#### 레이아웃 구조
```
┌─────────────────────────────────────────────┐
│ Header (고정)                               │
│ - 제목: "파일 복호화" + 아이콘              │
│ - 닫기 버튼 (X)                             │
├─────────────────────────────────────────────┤
│ Content (스크롤 가능)                       │
│ - 파일명 표시 영역                          │
│ - 안내 메시지                               │
│ - 비밀번호 입력 필드                        │
│ - 에러 메시지 영역 (복호화 실패 시)        │
├─────────────────────────────────────────────┤
│ Footer (고정)                               │
│ - 취소 버튼                                 │
│ - 복호화 버튼                               │
└─────────────────────────────────────────────┘
```

#### 자동 복호화 시도
1. 파일 읽기 시 `encrypted: true` 확인
2. localStorage에서 저장된 비밀번호 확인
   - 키: `encryption_password_${filePath}`
3. 비밀번호가 있으면 자동 복호화 시도
4. 성공 시: 다이얼로그 없이 콘텐츠 표시
5. 실패 시: 복호화 다이얼로그 표시

#### 복호화 실패 처리
- 에러 메시지: "비밀번호가 올바르지 않습니다."
- 재시도: 비밀번호 입력 필드 포커스, 기존 입력 지우기
- 취소: 파일 읽기 취소, 이전 화면으로 돌아가기

---

### 4. 암호화된 파일 표시

#### Sidebar 파일 목록
- **아이콘**: 🔐 (fa-lock)
- **위치**: 파일명 앞
- **스타일**: 
  - 기본: 회색 (#666666)
  - 호버: 진한 회색 (#333333)
- **툴팁**: "암호화된 파일 - 비밀번호가 필요합니다"

#### Header 파일명 표시
- 암호화된 파일: 파일명 옆에 🔐 아이콘 표시
- 예: `Document.md 🔐`

#### 파일 메타데이터
- `encrypted: true` 필드로 암호화 여부 표시
- 파일 목록 API 응답에 포함

---

### 5. 사용자 경험 최적화

#### 암호화 저장 워크플로우
1. **편집 모드 진입**
2. **콘텐츠 편집**
3. **저장 버튼 클릭**
   - 암호화 토글이 활성화되어 있으면 → 암호화 다이얼로그
   - 암호화 토글이 비활성화되어 있으면 → 일반 저장
4. **비밀번호 입력** (암호화 선택 시)
5. **암호화 진행** (로딩 표시)
6. **저장 완료** (성공 메시지)

#### 복호화 워크플로우
1. **암호화된 파일 클릭**
2. **자동 복호화 시도** (localStorage 확인)
   - 성공 → 콘텐츠 표시
   - 실패 → 복호화 다이얼로그
3. **비밀번호 입력** (필요 시)
4. **복호화 진행** (로딩 표시)
5. **콘텐츠 표시** (성공 시)

#### 에러 처리
- **암호화 실패**: "파일 암호화에 실패했습니다. 다시 시도해주세요."
- **복호화 실패**: "비밀번호가 올바르지 않습니다."
- **네트워크 오류**: "서버 연결에 실패했습니다. 나중에 다시 시도해주세요."

---

## 암호화 사용자 시나리오

### 시나리오 1: 중요한 개인 메모를 암호화하여 저장

**상황:**
- 사용자가 개인적인 일기나 중요한 메모를 작성 중
- 서버 관리자나 다른 사람이 볼 수 없도록 보호하고 싶음

**사용자 행동:**
1. 편집 모드에서 메모 작성
2. 저장 버튼 옆 드롭다운 메뉴 클릭
3. "암호화하여 저장" 선택
4. 암호화 다이얼로그에서 비밀번호 입력
5. 비밀번호 확인 입력
6. 비밀번호 강도 확인 (보통 이상 권장)
7. "비밀번호 기억하기" 체크 (선택사항)
8. "암호화하여 저장" 버튼 클릭

**결과:**
- 파일이 암호화되어 서버에 저장됨
- 파일 목록에 🔐 아이콘 표시
- 다음에 파일을 열 때 비밀번호 입력 필요 (기억하기 체크 시 자동 입력)

---

### 시나리오 2: 암호화된 파일을 열어서 읽기

**상황:**
- 사용자가 이전에 암호화하여 저장한 파일을 열고 싶음
- 비밀번호를 기억하기에 체크했거나, 비밀번호를 알고 있음

**사용자 행동:**
1. 사이드바에서 🔐 아이콘이 있는 파일 클릭
2. 시스템이 localStorage에서 저장된 비밀번호 확인
   - **케이스 A**: 비밀번호가 저장되어 있음
     - 자동으로 복호화 시도
     - 성공 시: 다이얼로그 없이 콘텐츠 표시
     - 실패 시: 복호화 다이얼로그 표시
   - **케이스 B**: 비밀번호가 저장되어 있지 않음
     - 복호화 다이얼로그 표시
3. 비밀번호 입력 (필요 시)
4. "복호화" 버튼 클릭

**결과:**
- 파일이 복호화되어 평문으로 표시됨
- 사용자가 내용을 읽고 편집할 수 있음

---

### 시나리오 3: 비밀번호를 잊어버린 경우

**상황:**
- 사용자가 암호화된 파일의 비밀번호를 기억하지 못함
- localStorage에도 저장되어 있지 않음

**사용자 행동:**
1. 암호화된 파일 클릭
2. 복호화 다이얼로그 표시
3. 비밀번호 입력 시도
4. 잘못된 비밀번호 입력
5. "비밀번호가 올바르지 않습니다" 에러 메시지 표시
6. 여러 번 시도하지만 실패
7. "취소" 버튼 클릭

**결과:**
- 파일을 열 수 없음
- 경고 메시지: "비밀번호를 잊어버리면 파일을 복구할 수 없습니다."
- 파일은 서버에 암호화된 상태로 남아있지만 접근 불가

**사용자 안내:**
- 암호화 다이얼로그에 항상 경고 메시지 표시
- 비밀번호를 안전한 곳에 보관하도록 안내

---

### 시나리오 4: 여러 파일을 연속으로 암호화 저장

**상황:**
- 사용자가 여러 개의 중요한 파일을 연속으로 암호화하여 저장하고 싶음
- 같은 비밀번호를 사용하고 싶음

**사용자 행동:**
1. 첫 번째 파일 편집 후 "암호화하여 저장" 선택
2. 비밀번호 입력 및 "비밀번호 기억하기" 체크
3. 저장 완료
4. 두 번째 파일 편집
5. "암호화하여 저장" 선택
6. 저장된 비밀번호가 자동으로 입력됨 (또는 제안됨)
7. 확인 후 저장

**결과:**
- 여러 파일을 같은 비밀번호로 암호화 가능
- 비밀번호 입력 시간 절약
- 일관된 보안 관리

---

### 시나리오 5: 암호화된 파일을 일반 파일로 변경

**상황:**
- 사용자가 암호화된 파일을 더 이상 보호할 필요가 없어짐
- 일반 파일로 변경하고 싶음

**사용자 행동:**
1. 암호화된 파일 열기 (비밀번호 입력)
2. 편집 모드 진입
3. 내용 수정 (선택사항)
4. 저장 버튼 클릭
5. 암호화 토글이 활성화되어 있으면 비활성화
6. 일반 저장 선택 또는 저장 버튼 클릭

**결과:**
- 파일이 평문으로 서버에 저장됨
- 파일 목록에서 🔐 아이콘 제거
- 다음에 열 때 비밀번호 불필요

---

### 시나리오 6: 암호화된 파일을 다른 사용자와 공유하지 않기

**상황:**
- 사용자가 암호화된 파일을 다른 사용자와 공유하고 싶지 않음
- 파일이 암호화되어 있어도 추가 보안 조치를 원함

**사용자 행동:**
1. 암호화된 파일 확인
2. 파일 목록에서 우클릭 (컨텍스트 메뉴)
3. "공유 설정" 또는 "권한 관리" 확인
4. 암호화된 파일은 기본적으로 비공개로 설정됨

**결과:**
- 암호화된 파일은 자동으로 비공개 처리
- 다른 사용자가 접근할 수 없음
- 서버 관리자도 암호화된 내용을 볼 수 없음

---

### 시나리오 7: 모바일에서 암호화된 파일 접근

**상황:**
- 사용자가 모바일 기기에서 암호화된 파일을 열고 싶음
- 비밀번호 입력이 불편할 수 있음

**사용자 행동:**
1. 모바일 브라우저에서 앱 접속
2. 암호화된 파일 클릭
3. 복호화 다이얼로그 표시 (모바일 최적화 레이아웃)
4. 비밀번호 입력 (모바일 키보드)
5. 복호화 버튼 클릭

**결과:**
- 모바일에서도 암호화/복호화 기능 사용 가능
- 터치 친화적인 인터페이스
- 비밀번호 입력 필드가 자동으로 포커스되어 키보드 표시

---

### 시나리오 8: 암호화 저장 중 네트워크 오류 발생

**상황:**
- 사용자가 파일을 암호화하여 저장하는 중 네트워크 연결이 끊김

**사용자 행동:**
1. 파일 편집 후 "암호화하여 저장" 선택
2. 비밀번호 입력 및 확인
3. "암호화하여 저장" 버튼 클릭
4. 암호화 진행 중 (로딩 표시)
5. 네트워크 오류 발생

**결과:**
- 에러 메시지: "서버 연결에 실패했습니다. 나중에 다시 시도해주세요."
- 파일은 아직 저장되지 않음
- 사용자가 재시도 가능
- 암호화된 데이터는 메모리에만 존재 (재시도 시 다시 암호화 필요)

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 1.0 | 2026-01-29 | 초기 작성 | - |
| 1.1 | 2026-02-02 | 암호화 관련 UI/UX 상세 설계 추가 | - |
| 1.2 | 2026-02-02 | 로그인 페이지 "로그인 없이 뷰어 시작" 버튼 및 비로그인 뷰어 흐름 추가 | - |
