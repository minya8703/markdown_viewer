# 환경 변수 설정 예시 파일
# 이 파일을 복사하여 .env로 이름을 변경하고 실제 값으로 수정하세요
# .env 파일은 .gitignore에 포함되어 Git에 커밋되지 않습니다

# Google OAuth 설정
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:8080/api/auth/google/callback

# JWT 설정
JWT_SECRET=your-secret-key-min-256-bits-long

# 프론트엔드 URL
FRONTEND_URL=http://localhost:3000

# 데이터베이스 설정
DB_USERNAME=root
DB_PASSWORD=your-password

# 파일 저장 경로 (선택)
# FILE_STORAGE_BASE_PATH=./data

# ---------- Redis (선택, 단일 인스턴스면 생략 가능) ----------
# Redis 사용 시에만 설정. 미설정 시 JWT 블랙리스트/캐시는 인메모리로 동작.
# 자세한 내용: docs/00_environment/01_REDIS_GUIDE.md
# REDIS_ENABLED=true
# REDIS_HOST=localhost
# REDIS_PORT=6379
# Redis 사용 시 Redis 자동구성을 켜려면 SPRING_AUTOCONFIGURE_EXCLUDE 를 설정하지 않음
